// let overallData;

// // function preload() {
// //     overallData = loadJSON('data/overall.json');
// // }

function setup() {
    let canvas = createCanvas(windowWidth, windowHeight);
    canvas.parent('canvas-container');
    background(220);
    // Drawing logic for overall data
}

function draw() {
    // Visualisierung für Overall Daten
}
