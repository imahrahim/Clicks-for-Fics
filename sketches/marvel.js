let marvelData;

function preload() {
    marvelData = loadJSON('data/marvel.json');
}

function setup() {
    let canvas = createCanvas(windowWidth, windowHeight);
    canvas.parent('canvas-container');
    background(220);
    // Drawing logic for Marvel data
}

function draw() {
    // Visualisierung für Marvel Daten
}
