// let bokuNoHeroData;

// function preload() {
//     bokuNoHeroData = loadJSON('data/boku_no_hero.json');
// }

function setup() {
    let canvas = createCanvas(windowWidth, windowHeight);
    canvas.parent('canvas-container');
    background(220);
    // Drawing logic for Boku No Hero data
}

function draw() {
    // Visualisierung für Boku No Hero Daten
}
